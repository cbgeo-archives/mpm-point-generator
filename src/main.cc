#include <iostream>
#include <memory>

#include "mesh.tcc"

int main() {

    //std::string filename =
    //"/home/cw646/mpm-point-generator/bin/test";
    //std::unique_ptr<Mesh<4>> mesh(new Mesh<4>());
    //mesh->get_vertices(filename);
    //mesh->output_vertices();
    //mesh->get_elements(filename);
    //mesh->output_elements();
}
