#include <iostream>
#include <memory>
#include "mesh.cc"

int main() {

    //Put gmsh file in project directory and un-comment out below

    //! Number of dimensions required
   // std::unique_ptr<Mesh<3>> mesh(new Mesh<3>());
   // mesh->readfile();
   // mesh->outputcoords();
}

